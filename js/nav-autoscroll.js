/**
 * nav-autoscroll.js — no-op for now. Nav has overflow:visible.
 * Links that don't fit will overflow off-screen. The autoscroll
 * logic would need a wrapper div to work. For now, rely on the
 * compact link sizes to fit most viewports.
 */
(function () {
  'use strict';
  // intentionally empty — nav overflow:visible, no scrolling needed
})();
